# szl-eclipse
Who verifies the verifier: mutation-testing for receipt verifiers. Ten attack classes (metric tamper, reorder, lane/key rename, truncation, type confusion, duplicate injection) against a golden chain; reports verifier sensitivity with a receipt. BLIND-SPOT state names what slipped. Fail-closed.
